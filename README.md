# Lucasweb78 Custom

## How do I install these formulae?
`brew install lucasweb78/custom/<formula>`

Or `brew tap lucasweb78/custom` and then `brew install <formula>`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
